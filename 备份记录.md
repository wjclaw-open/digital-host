# 备份记录

## 文件版本（最终稳定版）

| 文件 | 路径 |
|------|------|
| server.py | `digital-host/server_backup_final.py` |
| index.html | `digital-host/index_backup_final.html` |

## 恢复方法

如果 server.py 或 index.html 出问题，运行：
```cmd
copy server_backup_final.py server.py
copy index_backup_final.html index.html
```

然后重启服务。

## 版本说明（2026-04-30 16:42）

此版本稳定功能：
- ✅ `/chat` 浏览器直接聊天（不走队列，防止重复）
- ✅ `/push` 飞书等远程消息入队列（浏览器轮询显示）
- ✅ 双通道分离：sendChat() 和 autoPoll() 独立运行
- ✅ 用户问题和AI回答都有TTS语音播读
- ✅ `addMsg` 防重复：连续相同文本只显示一次
- ⚠️ NVIDIA API 有速率限制（429），连续请求会触发限速，等待5-10秒可恢复

## 端口
服务端口：8766